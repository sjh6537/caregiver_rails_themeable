<%# app/views/web/user/careds.html.erb %>

<div class="container">
    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header text-center bg-info text-white">
                    <h3 class=""><%= @title_sub %></h3>
                </div>
                <div class="card-body">
                    <% if @caregivers.any? %>
                    <ul class="list-group">
                        <% @caregivers.each do |user| %>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            <span>
                                <%= image_tag user.line_image, class: 'avatar avatar-sm rounded-circle me-2' %>
                                <%= user.line_name %>
                            </span>
                            <h6 class="text-muted ms-2"><%= t('Table.Name')%>：<%= user.name %><br /><%=t("Table.Phone")%>：<%= user.phone %></h6><br />
                        </li>
                        <% end %>
                    </ul>
                    <% else %>
                    <div class="text-center py-4">
                        <h4 class="text-danger"><%= t('Website.Note.Nobody_be_caregiver') %></h4>
                    </div>
                    <% end %>
                </div>
            </div>
        </div>
    </div>
</div>