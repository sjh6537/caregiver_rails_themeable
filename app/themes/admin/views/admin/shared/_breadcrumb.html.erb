<!-- breadcrumb -->
<div class="breadcrumb-header justify-content-between">
    <div class="my-auto">
        <div class="d-flex">
            <h4 class="content-title mb-0 my-auto"><%= @title || "管理後台" %></h4>
            <% if @title_sub %>
                <span class="text-muted mt-1 tx-13 mr-2 mb-0">/ <%= @title_sub %></span>
            <% end %>
        </div>
    </div>
</div>
<!-- breadcrumb end -->
